# playground

Dogfooding sandbox for `vitest-agent`. Not a real application — exists so the plugin, TDD orchestrator, and MCP tools have a live target during development.

The package runs as project `playground` in `vitest.config.ts` and contributes to coverage numbers on every test run.

Repo-internal context for maintainers lives in the root `CLAUDE.md`.
